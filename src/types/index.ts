export type NavList = {
  id: number;
  name: string;
  navigate_url: string;
};

export type SpeakerList = {
  id: number;
  name: string;
  role: string;
  img: string;
};

export type RundownList = {
  id: number;
  title: string;
  description: string;
  timeline: string;
};
